name: Deploy Jekyll to GitHub Pages

on:
  push:
    branches:
      - master

jobs:
  initialize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.1.4  # Ensure Ruby version is 3.1.4

      - name: Install Bundler
        run: gem install bundler -v 2.3.0

      - name: Configure Bundler Path
        run: bundle config set path 'vendor/bundle'

      - name: Install dependencies
        run: bundle install --jobs 4 --retry 3

  build:
    runs-on: ubuntu-latest
    needs: initialize
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Build with Jekyll
        run: bundle exec jekyll build --destination _site/profile
        env:
          JEKYLL_ENV: production

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GH_IO_PROFILE }}
          publish_dir: ./_site/profile
          cname: devtz007.github.io
          publish_branch: master  # Specify the branch to deploy to
